/**
 * - This function call is to initialize the send frame.
 */

uint8_t *frameInitializer(uint8_t destination, uint8_t source, uint8_t size);

/**
 * - This function call is to copy the sendBuffer frame to tempBuffer frame and returns
 *   the tempbuffer.
 */

isFrameValid();